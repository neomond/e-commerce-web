export const buttonStyles = {
  marginRight: 1,
  fontSize: 12,
  "@media (max-width: 600px)": {
    width: "auto",
    flex: 1,
  },
};

export const imageStyles = {
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  objectFit: "contain",
};

export const cardTextStyles = {
  display: "flex",
  justifyContent: "space-between",
};

export const cartItemCountStyles = {
  position: "absolute",
  top: 0,
  right: -10,
  background: "tomato",
  borderRadius: "50%",
  padding: "2px 8px",
  fontSize: "12px",
  color: "#fff",
};

export const cartBtnStyles = {
  minWidth: 0,
  padding: 0,
  position: "relative",
  "@media (max-width: 600px)": {
    marginTop: 0,
  },
};

export const navStyles = {
  display: "flex",
  justifyContent: "space-between",
  "@media (max-width: 600px)": {
    alignItems: "center",
  },
};

export const navTextStyles = {
  "@media (max-width: 600px)": {
    fontSize: 18,
  },
};

export const popUpEmptyStyles = {
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
};

export const listItemStyles = {
  display: "flex",
  alignItems: "center",
  justifyContent: "space-between",
};

export const fontStyles = {
  fontSize: 22,
  fontWeight: "600",
};
